apiVersion: v1
kind: Service
metadata:
  name: {{ include "sport-api.fullname" . }}
  labels:
    {{- include "sport-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sport-api.selectorLabels" . | nindent 4 }}
